# Copy to .env and fill in your values

# Required for nano-banana-pro skill (image generation)
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: GitHub CLI authentication (if not using gh auth login)
# GITHUB_TOKEN=your-github-token-here
