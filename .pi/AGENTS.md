# Pi Compatibility Shim

Follow the canonical Sapling contract in `../AGENTS.md`.

Pi-specific notes:

- The shared Sapling skill lives at `.pi/skills/sapling-brain/SKILL.md`.
- Prefer `brain/raw/`, `brain/wiki/`, `brain/ops/`, and `brain/outputs/` for all new work.
- If Pi hook support is unavailable, execute the same checks manually: read `AGENTS.md`, validate schemas, update the wiki, and track multi-session work with beads.
