# Granola Sync Configuration

# Path to save call notes (defaults to brain/calls/ relative to this repo)
# OUTPUT_PATH=/path/to/your/vault/brain/calls

# Delay in seconds before syncing (allows Granola to finish generating notes)
# SYNC_DELAY=60
